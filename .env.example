# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# --- Gmail (required for live site — stops spam) ---
#
# 1. Google Cloud → enable Gmail API → create OAuth Web client
# 2. Redirect URI: https://YOUR-SITE.vercel.app/api/gmail-setup/callback
# 3. Add test user: your sending Gmail (OAuth consent screen → Audience)
# 4. On Vercel set the vars below, redeploy
# 5. Visit: https://YOUR-SITE.vercel.app/api/gmail-setup?key=YOUR_SETUP_SECRET
# 6. Sign in → copy GMAIL_REFRESH_TOKEN to Vercel → redeploy again
GMAIL_USER=
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_SETUP_SECRET=make-up-a-long-random-password

# Fallback only (worse inbox placement than Gmail API above):
GMAIL_APP_PASSWORD=

# Never set to "Little Letter"
GMAIL_FROM_NAME=

# Resend — only with a verified custom domain
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# Stripe
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# Optional — pre-created Price IDs make checkout faster than price_data:
# STRIPE_PRICE_LETTER=price_...
# STRIPE_PRICE_CARD=price_...
# STRIPE_PRICE_MIX_ONE=price_...
# STRIPE_PRICE_MIX_MULTI=price_...

# Postgres (Vercel ↔ Supabase integration) — tracks free/paid sends by email.
# Needs a real Postgres URL (POSTGRES_URL). Do not use @vercel/postgres/Neon-only drivers.
POSTGRES_URL=

NEXT_PUBLIC_SITE_URL=https://sendlittleletter.vercel.app

# Optional — Facebook / Meta App ID for share debugger (fb:app_id).
# Create a free app at https://developers.facebook.com/apps/ then paste the ID.
NEXT_PUBLIC_FB_APP_ID=

# Google Analytics 4 — Measurement ID from Admin → Data streams (G-XXXXXXXX)
# Must be named NEXT_PUBLIC_GA_MEASUREMENT_ID (or NEXT_PUBLIC_GA_MEASUREMENT).
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Google AdSense — publisher ID (ca-pub-…) and the Display unit slot (digits only).
# Slot is NOT the same as the publisher ID. Create a Display ad unit in AdSense
# and paste data-ad-slot here. Ads appear above the footer; empty units stay hidden.
NEXT_PUBLIC_ADSENSE_CLIENT_ID=
NEXT_PUBLIC_ADSENSE_SLOT_END=

# YouTube Data API v3 — optional (song search works without it now).
# Enable “YouTube Data API v3” and add a key if you want the official API.
YOUTUBE_API_KEY=
# DEMO_MODE=true
# CRON_SECRET=  # Vercel sets this for scheduled letter sends (every 5 min)
